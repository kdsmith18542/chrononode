[verification]
checkpoint_size = 1000
hash_algorithm = "sha256"

[api]
port = 8080
api_key = "your-secret-api-key-here"

[api.rate_limiting]
requests_per_second = 100
burst_size = 200

[storage]
backend = "local_fs"
# backend = "ipfs"
# backend = "pinata"
base_path = "./data"

[index]
db_path = "./data/index.db"

[logging]
level = "info"
format = "json"

[metrics]
enabled = true
port = 9090

[adapters.baals]
url = "http://localhost:9070"

[adapters.bitcoin-light]
# Esplora-compatible endpoints (first URL is primary, others are fallback)
api_urls = ["https://mempool.space", "https://blockstream.info"]

[adapters.dogecoin]
# Provider mode: "blockcypher" (default) or "rpc"
# mode = "rpc"

# BlockCypher endpoint for DOGE chain data (mode="blockcypher")
api_url = "https://api.blockcypher.com"

# Optional: add additional compatible endpoints for failover
# api_urls = ["https://api.blockcypher.com"]

# Optional: paid token to raise DOGE API limits
# api_token = "your-blockcypher-token"
# Or set CHRONONODE_DOGE_API_TOKEN in the environment.

# JSON-RPC provider (mode="rpc"), e.g. QuickNode/GetBlock/Chainstack
# rpc_url = "https://your-doge-rpc-endpoint"
# Optional basic auth
# rpc_username = "user"
# rpc_password = "pass"
# Optional API key header auth
# rpc_api_key = "your-api-key"
# rpc_api_key_header = "x-api-key"

# --- Dormancy Detection (Phase 3) ---
[dormancy]
# Default threshold in blocks before an address is considered dormant
# 26280 blocks ≈ 5 years for Bitcoin (10-min blocks)
default_threshold_blocks = 26280

# Per-chain overrides
[dormancy.chain_thresholds]
# bitcoin = 26280
# dogecoin = 2103840  # ~4 years (1-min blocks)

# --- Attestation Submission (Phase 4) ---
[attestation]
# BaaLS API endpoint for submitting dormancy attestations
# baals_api_url = "http://localhost:18080"

# Path to the BaaLS account ed25519 key file (32 bytes raw seed)
# baals_key_path = "/etc/chrononode/baals.key"

# Automatically submit attestation when new dormant addresses are detected
auto_submit = true

# EVM RPC endpoint for Resurgence RewardDistributor (Arbitrum Sepolia testnet)
# evm_rpc_url = "https://sepolia-rollup.arbitrum.io/rpc"

# Resurgence RewardDistributor contract address
# evm_contract_address = "0xCDfd46512dA68e2eD555D1d0Ac09aB1Acf38f2Ed"

# Hex-encoded secp256k1 private key for signing EVM transactions (no 0x prefix)
# Keep this file readable only by the chrononode user (chmod 600)
# evm_private_key = "your-64-char-hex-private-key"

# EVM chain ID — 421614 for Arbitrum Sepolia, 42161 for Arbitrum One
# evm_chain_id = 421614

# Gas limit for EVM dormancy proof submission (default: 1_000_000)
# evm_gas_limit = 1_000_000
